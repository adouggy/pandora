<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/img_round_corner"
    android:padding="15dp"
    android:layout_margin="10dp">

    <ImageView
        android:id="@+id/img_friend_img"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:contentDescription="@string/defaultDescription"
        android:maxHeight="150dp"
        android:scaleType="fitCenter"
        android:src="@drawable/empty_profile" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/img_friend_img"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/txt_friend_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="200dp" />
        
        <TextView
            android:id="@+id/txt_friend_presence"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="200dp" />
        
        <Button 
            android:id="@+id/btn_friend_chat"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:text="@string/txt_friend_chat"
            android:background="@drawable/chat_btn_start"/>
    </LinearLayout>

</RelativeLayout>